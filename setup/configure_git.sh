#!/usr/bin/env bash

# dotfile contains secrets 
git config --global diff.tool vimdiff
git config --global difftool.prompt 0
git config --global merge.tool vimdiff
git config --global alias.s status

